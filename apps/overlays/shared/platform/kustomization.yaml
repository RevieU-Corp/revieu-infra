apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../../base/common/cluster-issuer.yaml
  - ../../prod/common/cloudflare-api-token-sealed.yaml

patches:
  - target:
      kind: ClusterIssuer
      name: letsencrypt-prod
    patch: |-
      - op: replace
        path: /spec/acme/email
        value: liweijun0302@gmail.com
